# Resume a stopped Multi-brand stage

Stop the previous writer first. Use its conversation, or paste the original stage
prompt with the same skill in a fresh conversation, then this block:

```text
Resume only the stage/write directory authorized by the original Multi-brand launch prompt. If that prompt is missing or the stage is ambiguous, ask which stage instead of inferring ownership from existing files.

Read ideas/multi-brand-content/research-protocol.md and your existing authorized outputs. Preserve credible records, stable IDs, negative findings and source-inspection notes. Recount files rather than trusting a stale total. Do not delete conversations/artifacts as a recovery strategy, and never inspect internal artifacts.

If a quota/access blocker remains, end partial with next_actions. Otherwise resume the recorded unfinished task. Research uses original public pages; Auditor opens only existing raw source_url values; Judge never browses. An uninspected source cannot become VERIFIED from memory. A raw change after audit requires a fresh audit and then a fresh Judge run.

No python -c, python stdin, inline code, scripts, scraping, .gemini/antigravity/brain, .system_generated, cache or outside-workspace reads. Use only the checker commands authorized by the original prompt. One owned structural repair and one rerun maximum, then stop if still blocked. Do not change scope/gates, expand access or take over another track.

Report what was preserved, newly completed, left unverified and still blocked.
```
