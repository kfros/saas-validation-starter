# Run Workflow / ICP Mapping

First enable browser access with `/browser`.
Then invoke `/workflow-mapping` and provide:

```text
Run Stage 1 Workflow / ICP Mapping for `ideas/deck-automation`.

Determine where the job actually occurs. Map candidate segments, triggers, inputs, outputs, tools, actors, buyers, recurrence evidence, manual effort, and channels through which Stage 2 prospects could be found.

Write only to:
`ideas/deck-automation/raw/workflow/`

Candidate ICPs from the hypothesis are hypotheses, not facts. Challenge them and add/remove candidates only when supported by evidence.
Do not build the Stage 2 prospect list yet.
Do not invent frequency.
Do not decide PASS/FAIL.
Finish only after structurally checking JSONL records against the canonical schema. All raw records must remain `PENDING`.
```
